"""Auth package.

Phase 2 populates this with jwt.py (token create/verify, password hashing)
and dependencies.py (get_current_user, permission checks). A working
`get_current_user` dependency already exists at `app.dependencies` for
modules that need it before Phase 2 lands; Phase 2 should move/align that
logic here per skills/BACKEND.md conventions if desired.
"""
